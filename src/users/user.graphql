type User {
  id: Int!
  firstName: String!
  lastName: String
  username: String!
  todoLists: [TodoList!]
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}

input CreateUserInput {
  firstName: String!
  lastName: String
  username: String!
  password: String!
}