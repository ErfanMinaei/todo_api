type AuthPayload {
  token: String!
  user: User!
}

type Mutation {
  login(input: LoginInput!): AuthPayload!
  register(input: RegisterUserInput!): AuthPayload!
}

input LoginInput {
  username: String!
  password: String!
}